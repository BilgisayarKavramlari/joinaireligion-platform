"use client"; import { useState } from "react"; import Link from "next/link";
export default function Page(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const onSubmit=async(e:React.FormEvent)=>{e.preventDefault();const r=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});setMsg(r.ok?'If your email exists, reset instructions are prepared.':'Try again later.');}; return <main className="min-h-screen p-6"><div className="mx-auto max-w-md rounded-xl border border-slate-700 bg-slate-900 p-6"><h1 className="text-3xl">Forgot password</h1><form onSubmit={onSubmit} className="mt-4 space-y-3"><input className="w-full rounded bg-slate-950 p-2" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><button className="w-full rounded bg-violet-600 p-2">Send reset link</button></form>{msg&&<p className="mt-2">{msg}</p>}<Link href="/login" className="mt-2 inline-block underline">Back to login</Link></div></main>;}
