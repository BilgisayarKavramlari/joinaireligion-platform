# Internationalization

Supported locales: `en`, `tr`, `es`, `de`, `fr`, `ar`, `ru`, `zh`.

- Browser language is detected on first visit.
- Manual language selector is available in header.
- Selected interface language is stored client-side (and can later be stored in user preferences server-side).
- Interface language and email language are separate preferences in the data model.
